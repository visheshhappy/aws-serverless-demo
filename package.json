{
  "name": "hello-serverless",
  "version": "1.0.0",
  "description": "sls invoke local -f hello",
  "main": "add.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^5.12.0"
  }
}
